{
  "name": "medref-contracts",
  "version": "1.0.0",
  "description": "MedRef Smart Contracts - Medical Records on Polygon",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy-all.ts --network hardhat",
    "deploy:amoy": "hardhat run scripts/deploy-all.ts --network amoy",
    "clean": "hardhat clean",
    "accounts": "hardhat accounts"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@types/node": "^20.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
